fn main() {
    // The ! means it's calling a rust macro
    // instead of a function.
    println!("Hello, world!");

    // interesting... semicolons are optional if followed by a }
    // println!("Hello, world!")
}
